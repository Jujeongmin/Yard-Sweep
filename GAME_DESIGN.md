# Yard Sweep 개정 기획서

초기 기획서를 기준으로 실제 개발 과정에서 확정되거나 변경된 내용을 기록한다. 별도로 적지 않은 시스템은 초기 기획을 유지한다.

## 현재 핵심 플레이 루프

1. 지역의 일반·희귀 오브젝트를 청소한다.
2. 코인과 보석을 획득한다.
3. 상점에서 장비와 플레이어 능력을 강화한다.
4. 진행도 100% 달성 시 다음 지역을 해금한다.
5. 3지역까지 완료한 뒤 1지역부터 반복 플레이한다.

## 장비 및 인벤토리 변경

인벤토리는 개별 장비 8개를 나열하던 방식에서 **카테고리 3칸** 방식으로 재설계되었다(숫자키 1~3, 클릭도 가능).

- **1번(청소)**: 기본 빗자루(무료, 시작 장착) → 더 큰 빗자루(코인 100) → 송풍기(코인 500). 낙엽·빈 캔·금 캔·골드 상자·보석 상자를 청소.
- **2번(잔디)**: 구리 낫(코인 300) → 금속 낫(코인 600) → 네온 낫(보석 150, 가장 넓고 빠름). 낫 계열은 전부 이동하며 사용 가능. **시작 시 빈 칸** — 사서 장착하기 전까지 잔디를 전혀 청소할 수 없다.
- **3번(돌)**: 곡괭이(코인 500, 정지 상태에서만 사용 가능) → 네온 곡괭이(보석 150, **이동하며 사용 가능**한 것이 차별점). **시작 시 빈 칸** — 사서 장착하기 전까지 돌을 전혀 청소할 수 없다.
- 카테고리별로 보유한 장비 중 하나만 "장착"할 수 있고, 상점에서 미보유 장비는 구매 즉시 자동 장착된다. 보유했지만 미장착인 장비는 "장착" 버튼으로 바꿀 수 있다.
- 앞 마당(지역 1)은 낙엽·캔만 있어 완주해도 코인 200 안팎인데, 잔디 칸을 채우는 구리 낫이 300코인이라 정원(지역 2)의 잔디 50개를 치우려면 앞 마당을 1~2바퀴 더 돌아야 한다. 의도된 페이싱인지 확인이 필요하다.
- 신규 저장 데이터는 코인 0, 보석 0으로 시작한다(과거의 테스트용 보석 300개 지급은 제거되었다).
- 지역은 정상 해금 규칙(이전 지역 100% 완료 시 다음 지역)을 따른다. 테스트용 전체 해금 코드는 넣었다가 확인 후 제거했다.

## 지역 진행 변경

- 새 지역은 코인 구매가 아니라 이전 지역 진행도 100% 달성으로 자동 해금된다.
- 지역 1 완료 시 지역 2, 지역 2 완료 시 지역 3이 열린다.
- 지역 3 완료 후 앞 마당부터 다시 플레이할 수 있다.
- 상점의 `지역` 탭에서 해금된 지역을 자유롭게 이동할 수 있다.
- 지역별 남은 오브젝트 수량과 진행률은 이동·새로고침 후에도 유지된다.
- 완료 보상을 받은 지역의 진행 데이터만 삭제되며, 재입장 시 새 오브젝트가 생성된다.
- 지역별 일반 오브젝트 수량은 초기 기획을 유지한다.
- 지역 완료 시 전용 UI와 효과음이 재생된다.
- 진행도(%) 표시는 반올림이 아닌 내림(`Math.floor`)을 사용한다. 오브젝트 총량이 큰 지역(예: 돌 정원)에서 반올림을 쓰면 마지막 1개가 남아도 화면에 100%로 보여 완료 카드가 안 뜨는 것처럼 보이는 문제가 있었다.
- 상단 HUD에 현재 지역 진입 후 경과 시간(mm:ss)을 표시한다.

## 지역별 비주얼 테마

세 지역이 같은 플레이 공간(지형·집·나무)을 공유하지만 지역 진입 시 하늘·안개·바닥·산책로 색상과 장식 오브젝트를 바꿔 지역마다 다른 분위기를 낸다.

- **앞 마당**: 기존 파란 하늘 + 초록 잔디 + 황토색 산책로.
- **정원**: 청록빛 하늘, 짙은 초록 잔디, 흩뿌려진 꽃 장식(색색의 꽃 36개)과 산책로 양옆 산울타리.
- **돌 정원**: 흙빛 하늘·안개, 회갈색 바닥·산책로, 바닥에 놓인 납작한 디딤돌 장식(18개)으로 마른 정원(zen garden) 느낌을 낸다. 처음엔 청소 가능한 `돌` 오브젝트와 똑같이 생긴 둥근 바위를 썼다가 서로 헷갈린다는 피드백을 받아 납작한 판석으로 바꿨고, 이후 우뚝 선 뾰족한 입석 바위도 추가했다가 다시 제거해 지금은 납작한 디딤돌만 남아있다.
- 장식은 미리 만들어 두고 해당 지역이 아닐 때는 `visible = false`로 숨긴다(청소 가능한 오브젝트가 아닌 순수 배경 장식이다).
- 집은 원래 오른쪽 벽이 산책로 가장자리(x -4)에 붙어 있어 정원 지역의 산울타리 라인(x ±4.3)이 벽을 관통했다. 집 전체(벽·지붕·창문·현관문·굴뚝·실내 일일 보물상자 포함)를 왼쪽으로 2유닛 옮겨(외벽 x -18..-6) 산책로·산울타리와 겹치지 않게 했다. 이동 충돌 벽(`wallRects`), 스폰 제외 범위(`houseBounds`), 현관문 자동 개폐 거리 기준도 함께 옮겼다.
- `돌`/`황금 돌` 오브젝트는 `houseBounds`를 참조하지 않는 별도의 고정 스폰 범위(x -18..-11)를 써서, 집을 옮긴 뒤 집 앞면(z -7.15)과 살짝 겹칠 뻔했다. z 시작점을 -5 → -6.5로 옮겨 세 지역 모두에서 동일하게 집과 겹치지 않도록 맞췄다(잔디/낙엽/캔 등 나머지 오브젝트는 이미 `randomOpenPosition()`으로 `houseBounds`를 자동 회피).

## 지역 반복 완료 보상

지역을 완료할 때마다(반복 플레이 포함) 코인·보석 보너스를 지급한다. 다음 지역 해금과 별개로 매번 지급된다.

| 지역 | 코인 | 보석 |
|---|---|---|
| 앞 마당 | 30 | - |
| 정원 | 60 | 2 |
| 돌 정원 | 120 | 5 |

- 코인 획득량 업그레이드는 완료 보상 코인에도 적용된다. 보석에는 적용되지 않는다.

## 코인/보석 표시 방식

- 코인·보석은 내부적으로 소수점을 유지한 채 누적된다(업그레이드 배율 등으로 생기는 소수점을 매번 반올림하면 장기적으로 손해가 누적되기 때문). 화면에는 `Math.floor`로 정수만 표시한다.
- 업적용 누적 스탯(`stats.coinsEarned`)은 표시가 정수여야 하므로 정수로 누적한다.

## 청소 중단/재개 규칙

- 조준(에임)에서 오브젝트가 벗어나거나 청소를 멈추면 진행 중이던 청소는 0%로 초기화된다.
- 다만 화면 경계에서 순간적으로 깜빡이는 것까지 중단으로 처리하면 작업음이 반복 재생되는 문제가 있어, 0.15초의 유예 시간(grace period)을 두고 그 안에 다시 잡히면 중단으로 치지 않는다.

## 미션과 업적

- 상점에 `미션`, `업적` 탭을 추가했다.
- **미션(단기 목표)** : 낙엽 100개 청소, 빈 캔 30개 수거, 마당 100% 청소, 5분 안에 지역 완료. 초기 기획의 "3개 무작위 로테이션" 대신, 4개 미션 풀 전체를 항상 동시에 노출하는 방식으로 단순화했다.
- **업적(장기 목표)** : 첫 청소 완료, 코인 1,000개 획득, 모든 지역 해금, 낙엽 10,000개 제거. 누적 스탯에서 진행도를 계산한다.
- 목표를 달성하면 버튼을 누를 필요 없이 그 즉시 자동으로 보상이 지급되고 화면에 알림 팝업이 뜬다(`checkMissionsAndAchievements()`가 오브젝트 청소·지역 완료 시점마다 모든 미션/업적 진행도를 검사). 미션은 보상 지급 후 진행도가 0으로 초기화되어 다시 반복되고, 업적은 한 번 지급되면 다시 초기화되지 않는다. 상점의 미션/업적 탭 버튼은 이제 클릭 가능한 "받기" 버튼이 아니라 진행도/완료 상태만 보여주는 표시용이다.
- 미션·업적 보상에도 코인 획득량 업그레이드가 코인 보상에 적용된다(보석 제외).

## 레벨 시스템

- 오브젝트를 하나 청소할 때마다 경험치 1%가 오르며, 100%마다 레벨이 1 오른다. 오브젝트 종류에 따른 차등은 없다.
- 레벨업 보상은 없다. 순수 진행도 표시/경쟁용 지표다.
- 별도 저장 필드 없이 누적 청소 개수(`stats.totalCleaned`)에서 파생 계산한다: `레벨 = floor(누적 청소 개수 / 100) + 1`, `진행도% = 누적 청소 개수 % 100`.
- 상단 HUD에 레벨과 경험치 바를 항상 표시한다.

## 랭킹 (Verse8 Game Server SDK 연동)

- 더 이상 뼈대가 아니라 `@agent8/gameserver`(`src/ranking.ts`)를 통해 실제로 연동되어 있다.
- 설정 패널에서 닉네임을 등록해야 랭킹에 이름이 오른다(등록 전에는 안내 문구만 표시). 닉네임 입력 없이 저장을 누르면 경고 문구가 뜬다.
- 레벨/경험치는 레벨이 바뀔 때만 서버로 보낸다(`reportScoreToPlatform()`). `updateHud()`는 오브젝트 하나마다 도는데 거기서 매번 보내면 호출이 과해져 예전엔 10분 스로틀을 걸어야 했고, 그 탓에 레벨업 직후 랭킹을 열어도 옛 점수가 보였다. 레벨업은 100개당 1회라 즉시 보내도 부담이 없다.
- **접속이 끝나면 곧바로 현재 레벨/경험치를 한 번 올려 `rankings` 행을 만든다.** 이게 없으면 신규/복귀 플레이어는 다음 레벨업(오브젝트 100개)까지 컬렉션에 행이 아예 없어 내 순위가 계속 "-"로 뜨고, 아무도 레벨업하지 않은 동안에는 상위 목록도 비어 보였다. "리더보드가 자주 안 뜬다"의 주원인이었다.
- `syncStats()`는 서버 미연결 상태에서도 값을 버리지 않고 대기열에 넣는다. 게임 시작 직후의 첫 보고는 접속이 끝나기 전에 오는 경우가 많은데, 예전에는 그걸 그냥 버리면서 `lastSyncedLevel`만 갱신해 버려 그 레벨 구간의 점수가 영영 올라가지 않았다. 접속이 끝나면 `initRanking()`이 대기열을 흘려보낸다.
- 닉네임이 없어 서버가 거절하면 30초 재시도를 걸지 않는다(재시도해도 똑같이 거절당한다). 값은 대기열에 남고 `setNickname()`이 등록 직후 직접 흘려보낸다.
- 상점의 `랭킹` 탭은 로비에서 미리 받아둔 캐시를 즉시 그리고, 뒤에서 다시 받아 조용히 교체한다(stale-while-revalidate). 프리페치가 성공하면 탭을 열기 전에 미리 그려둔다. 아직 접속 시도가 끝나지 않은 상태로 탭을 열면 "서버 미연결"을 바로 띄우지 않고 접속 결과를 기다린다 — 예전에는 로딩이 느린 기기에서 이 문구가 그대로 굳어 탭을 다시 열기 전까지 랭킹이 안 뜨는 것처럼 보였다.
- **`getTopRankings`와 `getMyRank`는 `Promise.allSettled`로 따로 받는다.** 예전엔 `Promise.all`이라 한쪽만 실패해도 목록이 통째로 사라졌고, 실제로 서버의 `getMyRank`가 `"Invalid collection count response."`를 던지는 동안 랭킹이 아예 안 떴다. `getMyRank`가 실패해도 상위 목록에 내 계정이 있으면 거기서 순위를 읽어 "내 순위" 칸을 채운다.
- 서버 `getMyRank`는 `level`/`exp`를 두 번 세던 것을 `score` 한 번으로 바꿨다(`getTopRankings`의 정렬 기준과 동일해진다). `countCollectionItems` 응답 형태가 런타임마다 달라 파싱에 실패하면 실제 행을 가져와 세는 쪽으로 물러선다(`countRankings()`).
- `#ranking-my-rank`는 `renderRankings()`가 소유한다. 예전엔 `updateHud()`도 같은 요소에 `Lv.N`을 써서, 랭킹을 띄운 뒤 오브젝트를 하나만 치워도 순위가 곧바로 사라진 것처럼 보였다.
- 설정의 "데이터 초기화"는 로컬 `localStorage` 저장을 지우는 것과 동시에 서버 쪽 계정 데이터(`resetAllData()`)도 함께 초기화한다(2단계 확인 필요: 한 번 누르면 확인 문구로 바뀌고, 다시 눌러야 실제로 초기화됨).

## 희귀 오브젝트

각 대상은 조건을 만족하는 지역에서 종류별 1~2개가 무작위 생성되며 지역 진행도에 포함된다.

| 오브젝트 | 등장 지역 | 사용 장비 | 보상 |
|---|---|---|---|
| 금 캔 | 모든 지역 | 빗자루·청소기 계열 | 코인 15 |
| 골드 상자 | 모든 지역 | 모든 장비 | 코인 40 |
| 보석 상자 | 모든 지역 | 모든 장비 | 보석 10 |
| 황금 돌 | 돌이 등장하는 지역 3 | 곡괭이 계열 | 코인 50 + 보석 5 |

- 코인 획득량 업그레이드는 희귀 오브젝트의 코인 보상에도 적용된다.
- 보석 보상에는 코인 획득량 업그레이드가 적용되지 않는다.

## 프리미엄 재화 아이템

- **코인 부스터** (보석 30개): 구매 시점부터 30분간 코인 획득량이 100% 추가된다(청소·지역 완료·미션·업적 보상 전부 포함, 코인 획득량 업그레이드와 중첩 적용). 상단 HUD에 남은 시간이 표시된다. 활성 중 다시 구매하면 남은 시간에 30분이 더해진다.
- **로봇청소기** (보석 200개): 장비 슬롯을 차지하지 않는 일회성 구매형 보유 아이템. 보유하면 게임 플레이 중 백그라운드에서 항상 작동하는 자율 AI 유닛으로, 플레이어와 무관하게 자기 자신의 월드 좌표를 기준으로 지역 전체에서 가장 가까운 미청소 오브젝트를 찾아 그쪽으로 스스로 이동한 뒤(2.2유닛/초), 도착하면 5초에 하나씩 청소하고 코인·보석을 지급한다. 장비 종류 제한 없이 모든 오브젝트를 청소할 수 있다. 밸런스를 위해 의도적으로 느리게(5초/개) 설계했다.
- 플레이어를 따라다니지 않는다 — 처음엔 카메라를 따라다니는 방식으로 만들었다가(1인칭이라 화면에 아예 안 보이는 문제도 있었음), 실제 자율 청소 로봇처럼 보이도록 독립적인 위치·이동 로직으로 다시 만들었다. 장애물 회피/경로 탐색 없이 목표 오브젝트를 향해 직선으로만 이동한다.
- 3D 모델(GLB)을 로드해 표시하며, 지역을 이동하면 이전 지역의 목표(오브젝트 참조)는 초기화되고 새 지역에서 다시 탐색한다.
- 실제로 오브젝트를 청소하는 동안(이동 중에는 재생하지 않음) 송풍기 작업음을 재생한다. 플레이어가 직접 송풍기를 사용할 때보다 조용하도록 기본 볼륨의 0.5배로 재생한다.

## 콤보 시스템

- 3초(`COMBO_WINDOW`) 안에 끊기지 않고 연속으로 오브젝트를 청소하면 콤보가 쌓인다. 3연속마다 보상 배율이 +0.25씩 붙고 최대 x3까지 오른다(`1 + min(floor(콤보수/3), 8) * 0.25`).
- 콤보 배율은 코인 획득량 업그레이드·코인 부스터와 중첩 적용되어 일반 오브젝트 보상에 곱해진다.
- 화면 중앙 하단에 콤보 수·배율·남은 시간 바가 표시되고, 콤보가 끊기면(시간 초과) 사라진다.

## 일일 보물상자

- 집 안 러그 위에 놓인 상자를 상호작용 힌트(F키/모바일 탭)로 하루 1회 개봉할 수 있다. UTC 자정 기준으로 리셋된다.
- 개봉 시 코인 150~500(균등 난수) + 50% 확률로 보석 2~5개를 즉시 지급한다.
- 개봉 가능한 동안에는 상자가 반짝이며 뚜껑이 열려 있고, 오늘 이미 열었다면 뚜껑이 닫힌 채 반짝임도 꺼진다.

## 튜토리얼

- 최초 플레이 시 3단계 튜토리얼이 순차적으로 진행된다: ①WASD로 4유닛 이동 ②좌클릭으로 오브젝트 3개 청소 ③Tab으로 상점 열기. 각 조건을 만족하면 자동으로 다음 단계로 넘어간다.
- 진행 상황은 저장되어(`tutorialStep` 등) 이후 접속 시 이어서 진행하거나, 이미 완료했다면 다시 뜨지 않는다.

## 상점 경제 확장 (VX 상점 · 광고 · 골드 교환)

- **골드 교환**: 보석 10개 → 코인 100개, 보석 100개 → 코인 1,000개로 즉시 교환 가능. 보석이 남을 때 코인으로 바꿔 쓸 출구를 마련한 것.
- **지역완료 2배 보상**: 지역을 완료하면 완료 카드에 "광고 보고 2배 보상 받기" 버튼이 뜬다. 실제 광고 SDK 연동 전까지는 클릭 시 1.2초의 "재생 중" 표시 후 보상이 2배로 지급되는 시뮬레이션이다. `광고 제거`를 구매한 경우에는 이 과정 없이 지역 완료 즉시 자동으로 2배가 지급된다.
- **VX 상점**(보석 100/500/1,200개 팩, 광고 제거): 실제 결제 연동 전까지는 클릭 시 전부 "출시 후 이용 가능" 안내만 뜨는 스텁이다. `광고 제거` 구매가 실제로 연결되면 콜백에서 `adsRemoved = true; persist(); refreshShop();`만 호출하면 되도록 적용 로직 자체는 이미 동작한다.

## 리워드 광고 (Verse8 Ads)

`@verse8/ads` SDK를 쓰며, 공식 문서(docs.verse8.io/ko/docs/ads/intro)의 규칙을 따른다.

- 광고는 두 곳에서만 뜬다: 지역 완료 카드의 `region-double-reward`(보상 2배), 프리미엄 탭의 `gem-reward-30`(30분 쿨다운마다 보석 30개).
- 반드시 유저의 클릭/탭에서만 호출하고 자동 재생하지 않는다. SDK가 한 번에 하나의 광고만 띄우므로 `isAdBusy()`로 중복 호출을 막고 버튼을 비활성화한다. `unsupported_env`가 오면 해당 광고 버튼을 아예 숨긴다.
- `showRewarded()`에 `timeoutMs`를 주지 않는다(SDK 0.5.0). 값을 생략하면 SDK가 타이머를 걸지 않고 호스트 응답을 무한정 기다린다. 제한을 걸어두면 유저가 광고를 끝까지 본 뒤 결과가 도착해도 이미 `timeout` 실패로 처리돼 보상이 날아간다.
- 결과 분기는 `rewarded` / `dismissed` / `failed` 세 가지를 구분한다. 특히 `busy`는 조용히 무시하고, `timeout`·`platform_error`는 "광고를 불러오지 못했습니다"로 안내한다 — 유저가 광고를 닫은 게 아닌데 "끝까지 시청해주세요"라고 하면 안 되기 때문이다.
- **지급은 서버 검증 후에만 이뤄진다.** 보석은 실제로 판매하는 재화라 문서가 서버 검증을 요구하는 대상에 해당한다. 클라이언트는 SDK가 준 `requestId`를 서버 `claimAdReward(placementId, requestId)`로 넘기고, 서버가 `https://ads-verifier.verse8.io/ads/status`를 조회해 `verified`일 때만 지급한다(`pending`이면 1.5초 간격으로 최대 4회 재시도).
- 지급량은 서버의 `AD_PLACEMENTS` 테이블에서만 나온다. 클라이언트가 보낸 값이나 verifier 응답의 `reward.amount`(문서상 UX 힌트일 뿐)는 쓰지 않는다. 보석은 VX 구매와 동일한 `crystals` 계정 상태에 적립되고, 클라이언트는 서버 값이 더 크면 그걸 채택한다.
- 재사용 방지는 계정 상태의 `adClaimedRequests`에 이미 지급한 `requestId`를 쌓아 검사한다(최근 200건 보관). 검증 대기 중 중복 요청이 들어올 수 있어 검증 전후로 두 번 확인한다. 다만 이건 DB 유니크 제약이 아니라 read-modify-write라, 완벽한 원자성은 아니다.
- `region-double-reward`는 지역 완료 판정 자체가 클라이언트에 있어 서버가 배수 액수를 알 수 없다. 그래서 서버는 "광고를 실제로 봤는가"만 검증하고(테이블상 보석 0), 코인·보석 2배 적용은 클라이언트가 한다. 코인은 판매 재화가 아니라 문서 기준 "low-stakes"에 해당한다.
- 서버 미연결이면 지급하지 않는다(fail-closed).

## 로딩 최적화

배포 용량이 27MB였고 그중 대부분이 에셋이라, 첫 진입이 특히 모바일에서 느렸다. 세 갈래로 줄였다.

- **미사용 에셋 제거**: 도구를 GLB 로드에서 코드 생성 방식으로 바꾸면서 남은 잔재(`Pick.glb`, `Sickle_0/1.glb`, `Broom_2.glb`, `vacuum_cleaner.glb`, `Vacuum.png`)와 점프 삭제로 쓰이지 않게 된 `jump-land-sound.mp3`를 지웠다. 약 3MB.
- **GLB 압축** (`gltf-transform optimize`): `Vacuum` 5.8MB→1.1MB, `PickSkin` 6.5MB→2.0MB(텍스처 1024²→512²), `RobotVacuum` 268KB→132KB. 모델은 `showToolModel()`에서 `1.35 / max(size)`로 정규화되므로 절대 크기는 무관하고 비율만 유지되면 된다. 교체 전 원본과 정규화 바운딩박스를 비교해 오차가 0.00005 이하임을 확인했다.
- **`SickleSkin.glb`은 압축하지 않았다.** 같은 파이프라인을 태우면 정규화 바운딩박스가 X −5.9%, Z −20% 달라진다(원본에 있던 빈 노드가 `prune` 단계에서 사라지면서 박스가 줄어드는 것으로 보인다. `--no-simplify`, 양자화 해제 모두 해봤지만 동일). 지오메트리 자체는 멀쩡하겠지만 네온 낫의 `gltfToolOffsets` 값이 기존 바운딩박스 기준으로 손수 맞춰진 것이라, 교체하면 화면상 위치가 틀어진다. 500KB 아끼자고 프리미엄 아이템 외형을 흔들 이유가 없어 원본을 유지했다.
- **BGM 지연 로딩**: 3곡 합계 9.6MB를 로딩 화면에서 전부 `canplaythrough`까지 기다리고 있었다. `preload='none'`으로 두고 현재 지역 곡 1개만 미리 받으며, 나머지는 `playRegionBgm()`이 그 지역에 들어갈 때 받는다.
- **번들 코드 분할**: three.js(gzip 153KB)를 게임 코드(gzip 61KB)와 별도 청크로 뺐다. 게임 로직만 고쳐 배포해도 유저는 three 청크를 캐시에서 재사용한다.

- **오디오 재인코딩**: 원본 BGM 3곡이 전부 256kbps였다. 낮은 볼륨(기본 0.28)으로 계속 반복되는 배경음악에 과한 비트레이트라 96kbps로 낮췄다(9.2MB→3.4MB). 효과음은 `broom-sound`만 320kbps로 튀어 있었고 나머지는 192kbps였는데, 짧은 타격음은 저비트레이트에서 아티팩트가 나기 쉬워 BGM보다 여유를 둬 128kbps로 맞췄다(0.6MB→0.3MB). 변환 후 브라우저에서 10개 파일 전부 디코딩되고 재생 길이가 원본과 일치하는 것을 확인했다.

- **세로 모드에선 로딩을 시작하지 않는다**: `#loading-screen`이 z-index 1000이라 회전 안내(`#rotate-screen`, z-index 100)를 덮고 있었고, 그 뒤에서 에셋 다운로드가 그대로 돌았다. 세로로 들어온 유저는 회전 안내를 못 본 채 진행률만 보게 되고, 세로로 방치하면 데이터만 쓰였다. 이제 세로에선 CSS로 로딩 화면을 숨겨 회전 안내가 보이게 하고, `startAssetLoadWhenReady()`가 가로로 돌린 뒤에 다운로드를 시작한다(플래그로 1회만 실행, `orientationchange`/`resize`/미디어쿼리 `change` 셋 다 걸어 기기별 이벤트 차이를 흡수).

결과적으로 에셋 27MB→9.2MB, 초기 전송량은 프로덕션 빌드 실측 6.3MB다.

## 사운드 시스템

- 지역 1~3에 각각 별도의 BGM을 사용한다.
- 빗자루, 진공청소기, 낫, 곡괭이 계열은 서로 다른 반복 작업음을 사용한다.
- 모든 UI 버튼에 버튼음을 사용한다.
- 지역 완료 시 전용 완료음을 사용한다.
- 실제 청소 대상이 범위 안에 있을 때만 작업음이 재생된다.
- 코인·보석을 획득하는 모든 순간(청소, 지역 완료, 미션·업적 수령)에 코인 획득음이 재생된다.
- 이동 중일 때만 발걸음 소리가 루프 재생되고, 멈추면 즉시 정지한다.
- 점프가 삭제되어 점프음은 없으며, 착지음(점프·착지음 파일)도 함께 제거되었다.

## 이동/조작 변경

- 점프 기능을 완전히 삭제했다(PC `Space` 키, 모바일 점프 버튼, 관련 중력·착지 로직 모두 제거). 플레이어는 항상 지면 높이에 고정된다.
- 모바일에서 전체화면을 켰다 껐다 반복하면 카메라가 계속 앞으로 밀리는 버그가 있었다. 브라우저가 전체화면 전환 시 터치를 `pointerup`/`touchend`가 아닌 `pointercancel`/`touchcancel`로 끊는데, 조이스틱과 시점 드래그 입력이 이 이벤트를 받지 않아 이동값이 0으로 리셋되지 않고 그대로 남아있던 게 원인이었다. 조이스틱에 `pointercancel`, 캔버스 시점 드래그에 `touchcancel` 리스너를 추가하고, `fullscreenchange` 핸들러에서도 안전장치로 입력값을 직접 리셋하도록 고쳤다.
- 반응형 분기 기준을 화면 **너비**에서 **포인터 종류**로 바꿨다. 기존에는 터치 컨트롤 CSS가 `(max-width: 850px) and (orientation: landscape)` 안에만 있어서, 태블릿 가로(iPad 1024px 등)에서는 `#mobile-controls`가 보이는데도 `#joystick`/`#clean-button`의 위치 CSS가 통째로 빠져 `position: static`으로 화면 밖에 밀려났다(조이스틱 1024×0, 청소 버튼 121×29, 둘 다 y=768) — 태블릿에서 이동·청소 자체가 불가능했다. 반대로 마우스를 쓰는 데스크톱에서 창을 850px 이하로 좁히면 조이스틱이 튀어나오고 WASD 안내가 사라졌다. 미디어쿼리 4개와 JS `usesMobileControls()`에서 너비 조건을 모두 제거하고 `(pointer: coarse)` / `(pointer: fine)`만 쓰도록 통일했다. CSS와 JS의 판별 조건이 어긋나면 "터치 UI는 뜨는데 포인터 락을 거는" 불일치가 생기므로 둘을 항상 같이 맞춰야 한다.
- 노치 대응을 추가했다. `index.html`의 viewport meta에 `viewport-fit=cover`를 넣고(이게 없으면 `env()`가 항상 0을 반환한다), `:root`에 `--safe-t/r/b/l` 변수를 정의해 HUD 패딩, 조이스틱 left/bottom, 청소 버튼 right/bottom, 툴 힌트 bottom에 `calc()`로 더했다. 노치 없는 기기·미지원 브라우저에서는 0px으로 떨어져 기존 레이아웃이 그대로 유지된다.
- 모바일에서 지역완료 보상 카드가 아예 뜨지 않는 버그가 있었다. `#start`(시작 화면)와 `#region-complete`(지역완료 카드)가 같은 `.start-card` 클래스를 공유하는데, `@media (pointer: coarse)`에서 모바일 시작 화면을 숨기려고 `.start-card { display: none; }`로 클래스 전체를 숨겨버려 지역완료 카드까지 함께 숨겨졌던 것이 원인이었다. 해당 규칙을 `#start`로 좁혀서 고쳤다.

## UI 변경

- 좌측 상단에 코인과 보석 잔액을 항상 표시한다.
- 코인 부스터가 활성화되어 있으면 코인·보석 옆에 남은 시간 배지가 표시된다.
- 상점은 `장비`, `강화`, `프리미엄`(코인 부스터·로봇청소기·보석→골드 교환), `VX 상점`(실결제 보석 팩·광고 제거, 전부 스텁), `지역`, `미션`, `업적`, `랭킹` 탭으로 구성된다.
- 상단 인벤토리는 개별 장비 8개 나열 대신 청소/잔디/돌 3개 카테고리 슬롯으로 표시한다.
- 낙엽 모양을 4개 꼭짓점의 단순한 별 모양에서 5개 꼭잎(위 1개, 좌우 상단·하단 각 1개)과 줄기가 있는 단풍잎 실루엣으로 다시 그렸다.
- 낙엽은 **평면 1장 구조를 유지하면서 오크 잎 사진 텍스처만 입혔다.** 받은 오크 잎 GLB를 그대로 인스턴싱하는 방안을 먼저 시도했지만, 포토그래메트리 스캔이라 9.6MB·187,000정점이고 압축 한계까지 줄여도(`--simplify-ratio 0.01`에 오차를 0.3까지 올려도 63,804 렌더정점에서 더 안 줄어든다 — 스캔이라 셸이 분리돼 심플라이어가 경계를 못 넘는다) 개당 약 2.5만 정점이라, 지역당 100개면 264만 정점이 된다. 실루엣은 어차피 도형으로 만들 수 있으니 GLB에서는 잎 표면(잎맥·색 얼룩)만 텍스처로 가져오는 쪽이 낫다고 판단했다.
- 텍스처는 GLB의 UV 아틀라스에서 잎 안쪽 영역만 잘라내고, 주맥이 가장자리에 치우쳐 있어 상하 반전본을 이어 붙여 주맥이 중앙에 오도록 만들었다(`leaf-texture.png`, 256×148, 72KB). 아틀라스라 잎이 흩어져 있어 통째로 평면에 붙이면 다른 잎 조각이 같이 보인다.
- 실루엣은 `createLeafShape()`가 그리는 오크형(끝이 뾰족한 타원 + 짧은 잎자루) `Shape`이 담당한다. `ShapeGeometry`의 UV는 도형 좌표를 그대로 쓰기 때문에 텍스처가 잘린다 — `remapUv()`가 바운딩박스 기준 0~1로 다시 매핑한다.
- 지오메트리는 전 낙엽이 하나를 공유하고(`leafGeometry`), 재질만 개체별로 만들어 `leafColors` 색조를 살짝 섞는다. 실측: 개당 48정점, 106개 합계 5,088정점 — 3D 모델안(264만)의 1/519다.
- 잔디를 평면 5장짜리 코드 생성 메시에서 고사리 GLB 3종(`Fern-0/1/2.glb`)으로 교체했다. 개체마다 3종 중 하나를 무작위로 고르고 y축 회전과 0.85~1.15배 크기를 흔들어 같은 모델이 반복되는 티를 줄인다. 원본 3종의 크기·중심이 제각각이라 프리로드 시 높이 0.85로 정규화하고 밑동이 y=0에 오도록 맞춰 둔 프로토타입을 만들어 두고, 생성 시엔 그것만 `clone()`한다. 그림자는 끈다 — 지역당 50개 × 약 6천 정점이라 켜면 섀도우 패스에서 같은 양을 다시 그리는데 발치의 작은 식물이라 얻는 게 거의 없다(이전 평면 잔디도 꺼져 있었다).
- `enterRegion()`이 에셋 프리로드보다 먼저 실행되므로 첫 진입 시 잔디는 기존 평면 메시로 만들어진다. 고사리 로드가 끝나면 `upgradeGrassVisuals()`가 아직 청소되지 않은 잔디의 겉모습만 갈아끼운다(그룹은 유지해 청소 진행도·위치를 보존하고, 버려지는 평면 메시의 geometry/material은 dispose한다).
- 상점·설정 패널(`.shop-panel`)은 flex 컬럼이다. 이전에는 일반 블록이라 헤더 높이 + 탭 높이 + 본문 `max-height: 55vh` + 푸터의 합이 패널의 `max-height: 88vh`를 넘으면 `overflow: hidden`에 걸려 푸터가 그대로 잘렸다. 가로 모드 폰(예: 844×390)에서 한국어도 30px 잘렸고, 글자 높이가 큰 일본어·번체는 44px까지 넘쳤다. 본문에 `flex: 1 1 auto; min-height: 0`을 줘서 헤더·탭이 커진 만큼 본문이 줄고 스크롤되도록 했다.

## 설정

- 상단 `⚙` 버튼 또는 `T` 키로 설정 패널을 열고 닫는다. 상점(Tab)과 상호 배타적으로 열리며(하나를 열면 다른 하나는 자동으로 닫힘), 여는 동안 포인터 락이 해제되고 청소가 중단된다.
- **BGM/효과음 볼륨**: 각각 0~100% 슬라이더. 기존에 하드코딩되어 있던 사운드별 기본 볼륨(예: BGM 0.28, 버튼음 0.45, 청소 작업음 0.6 등)에 곱해지는 배율로 적용해, 기존 사운드 간 상대적인 믹스 비율은 그대로 유지된다. 슬라이더를 움직이면 즉시 반영된다.
- **마우스 감도**: 50~200% 슬라이더(기본 100%). PC 마우스 이동과 모바일 터치 드래그의 시점 회전 속도에 곱해지는 배율이다.
- **진동** (모바일 전용 항목, PC에서는 숨김): 켜져 있으면 오브젝트를 하나 청소 완료할 때마다 짧게 진동한다(`navigator.vibrate`).
- **언어**: 한국어/English/日本語/繁體中文 4개 버튼으로 전환하면 게임 내 모든 텍스트(인벤토리, 상점 전 탭, 미션·업적, 알림, 지역 완료 카드, 힌트, 설정 패널 자체 등)가 즉시 해당 언어로 바뀐다. 첫 실행 온보딩 화면에도 같은 4개 버튼이 있다. `src/i18n.ts`에 ko/en/ja/zh-Hant 전체 문자열 딕셔너리(각 200키)와 `t(key, vars)` 템플릿 함수가 있고, 지원 목록은 `LOCALES` 배열 하나로 관리한다(`isLocale()`로 저장값을 검증해, 지원하지 않는 코드가 저장돼 있으면 기본값으로 되돌린다 — 그러지 않으면 `t()`가 키 문자열을 그대로 화면에 뱉는다). `gameData.ts`의 도구/오브젝트/지역/미션/업적 `name`·`label` 필드는 실제 표시 문자열이 아니라 i18n 키를 담고 있어 `t()`로 해석해야 한다. 정적 마크업은 `data-i18n`/`data-i18n-aria` 속성으로 표시하고, `applyLocale()`이 페이지 로드 시 한 번과 언어 버튼 클릭 시마다 전체를 다시 그린다.
- 모든 설정값은 기존 저장 데이터(`localStorage`)의 같은 블롭 안에 `settings` 필드로 함께 저장되어 새로고침 후에도 유지된다.

## 저장되는 데이터

- 코인과 보석 (소수점 포함)
- 현재 지역과 해금된 최고 지역
- 해금 장비, 카테고리별 장착 장비(`equipped`)
- 플레이어 업그레이드 레벨
- 프리미엄 장비 구매 상태, 광고 제거 구매 여부(`adsRemoved`)
- 코인 부스터 만료 시각, 로봇청소기 보유 여부
- 일일 보물상자 마지막 개봉 일자(`chestDayClaimed`)
- 누적 스탯(청소 개수 등), 미션 진행도, 업적 수령 여부, 튜토리얼 진행 단계
- 설정(언어 선택값, BGM/효과음 볼륨, 마우스 감도, 진동 여부)
- 닉네임과 글로벌 랭킹 자체는 로컬이 아니라 Verse8 Game Server(`@agent8/gameserver`)에 저장된다.

## 아직 추가 구현이 필요한 초기 기획

- 일반 장비 외형 스킨 시스템
- VX 상점 실결제 연동(보석 팩, 광고 제거) — 현재는 클릭 시 "출시 후 이용 가능" 안내만 뜨는 스텁
- 지역완료 2배 보상의 실제 광고 SDK 연동 — 현재는 1.2초 대기 후 지급되는 시뮬레이션
- 잔디/돌 카테고리가 시작부터 완전히 빈 칸이라 초반 진입 장벽이 큰 편 — 밸런스 재검토 필요
